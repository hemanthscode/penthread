/**
 * User Model Reference
 * 
 * Reuses the User model from auth module for consistency.
 * All user-related database operations use the same model.
 * 
 * @module modules/users/model
 */

import User from '../auth/auth.model.js';

export default User;
